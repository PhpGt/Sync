{
	"name": "phpgt/sync",
	"description": "Synchronise two directories.",

	"require": {
		"php": ">=7.2",
		"webmozart/glob": "4.*"
	},

	"require-dev": {
		"phpunit/phpunit": "8.*"
	},

	"autoload": {
		"psr-4": {
			"Gt\\Sync\\": "./src"
		}
	},

	"autoload-dev": {
		"psr-4": {
			"Gt\\Sync\\Test\\": "./test/unit"
		}
	},

	"keywords": [
		"rsync",
		"synchronise",
		"synchronize",
		"copy",
		"recursive",
		"filesystem",
		"file",
		"files"
	]
}